import { Form } from "../model/form"

export const saveFormDetails = async (formData: Form) => {
    try {
         
    } catch (error) {
        console.error("Error submiting form data", error);
    }
}